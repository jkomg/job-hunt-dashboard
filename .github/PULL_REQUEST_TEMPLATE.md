## Summary
<!-- What does this PR do? 1-3 bullet points. -->

## Notion changes
<!-- List any Notion database changes (new databases, new properties) and whether they're already applied. -->
- [ ] No Notion changes required
<!-- OR -->
- [ ] New database: `DB_NAME` (ID: `xxx`) — add `NOTION_XYZ_DB=xxx` to `.env`
- [ ] New property added to existing database: `Property Name` on `Database Name`

## Test plan
- [ ]
- [ ]

## Checklist
- [ ] `.env.example` updated if new env vars added
- [ ] README updated if setup steps changed
